// API Status
export enum Status {
  LOADING = "LOADING",
  SUCCESS = "SUCCESS",
  ERROR = "ERROR",
  INFO = "INFO",
}

// Sorting
export enum SortOrder {
  ASC = "ASC",
  DESC = "DESC",
}

// HTTP Methods
export enum HttpMethod {
  GET = "GET",
  POST = "POST",
  PUT = "PUT",
  PATCH = "PATCH",
  DELETE = "DELETE",
}

// Sizes
export enum Size {
  SM = "SM",
  MD = "MD",
  LG = "LG",
  XL = "XL",
}